git log -p -1
git log --stat

cd dir
cd ..
mkdir
notepad arquivo.txt ctrl + s

clear

git init #(master)

vim arquivo.txt
esc :wq enter

git status
git add arquivo.txt
git commit -m 'comandos adcionados'


git add .
git commit -a -m 'commitando varios arquivos de 1 vez'

git rebase interface # organiza linearmente os commits para merge
notepad .gitignore # digitar arquivos a serem ignorados

git reset HEAD arquivo.txt // estagio 2 > 1

start notepad++ arquivo.txt


git reset HEAD~x # volta x commits

git reset HEAD~1 --soft # volta mais de leves



git checkout <codigo commit ou brench>
git checkout -b interface

git merge interface # interface para branch atual



